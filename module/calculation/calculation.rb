# Все что относится к расчету по документам тут
class Documents_calc
  # Узнаем тип документа
  def type_documents(id=[])

  end
  # По типу документа подбираем формулу
  def formula_calc(type_doc=[])

  end
  # Создаем новую формулу и присваиваем к типам документа
  def formula_add(formula,type_doc=[], all=FALSE)

  end
  # Производим сам расчет
  '''
    Входные параметры:
      doctype or id_doc
  '''
  def calc_doc(*params)

  end
  # Поиск формулы
  def search(formula,nomer=[],type=[],all=FALSE)

  end
end